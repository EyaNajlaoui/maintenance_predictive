services:
  - type: web
    name: prognosia
    runtime: python
    plan: free
    buildCommand: pip install -r website/requirements.txt
    startCommand: gunicorn --pythonpath website app:app --bind 0.0.0.0:$PORT --timeout 120
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
